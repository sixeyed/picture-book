{
  "name": "picture-book",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build:site": "eleventy",
    "build:images": "node scripts/build-images.mjs",
    "dev": "wrangler pages dev build"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sharp": "^0.33.0"
  }
}
